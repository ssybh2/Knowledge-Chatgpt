{
  "name": "teddy-memory-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "smoke": "node --check src/index.js && node --check src/worker.js && node --input-type=module -e \"await import('./src/server.js'); await import('./src/memory-client.js'); await import('./src/http-handler.js'); await import('./src/worker.js')\"",
    "cf:dry-run": "wrangler deploy --dry-run --outdir dist",
    "start": "node src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
