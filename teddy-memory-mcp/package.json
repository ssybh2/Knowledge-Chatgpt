{
  "name": "teddy-memory-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "smoke": "node --check src/index.js && node --input-type=module -e \"await import('./src/server.js'); await import('./src/memory-client.js')\"",
    "start": "node src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.1.5"
  }
}
