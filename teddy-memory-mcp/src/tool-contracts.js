export const TOOL_NAMES = ['get_context', 'search_memory', 'get_conversation'];

export const READ_ONLY_ANNOTATIONS = Object.freeze({
  readOnlyHint: true,
  destructiveHint: false,
  idempotentHint: true,
  openWorldHint: false,
});
