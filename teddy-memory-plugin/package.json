{
  "name": "teddy-memory-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "smoke": "node --check src/worker.js && node --check scripts/live-smoke.mjs && node --check scripts/oauth-login.mjs && node --check scripts/chatgpt-compat.mjs && node --check scripts/migrate-safe-snapshots.mjs && node --input-type=module -e \"await import('./src/worker.js'); await import('./scripts/live-smoke.mjs'); await import('./scripts/oauth-login.mjs'); await import('./scripts/chatgpt-compat.mjs'); await import('./scripts/migrate-safe-snapshots.mjs')\"",
    "live:smoke": "node scripts/live-smoke.mjs",
    "oauth:login": "node scripts/oauth-login.mjs",
    "compat:chatgpt": "node scripts/chatgpt-compat.mjs",
    "migrate:safe-snapshots": "node scripts/migrate-safe-snapshots.mjs",
    "cf:dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "jose": "6.2.10",
    "zod": "^4.1.5"
  },
  "devDependencies": { "wrangler": "4.127.1" }
}
