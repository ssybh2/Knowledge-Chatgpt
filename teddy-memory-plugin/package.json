{
  "name": "teddy-memory-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "smoke": "node --check src/worker.js && node --check scripts/live-smoke.mjs && node --input-type=module -e \"await import('./src/worker.js'); await import('./scripts/live-smoke.mjs')\"",
    "live:smoke": "node scripts/live-smoke.mjs",
    "cf:dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.1.5"
  },
  "devDependencies": { "wrangler": "4.127.1" }
}
