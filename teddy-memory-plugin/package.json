{
  "name": "teddy-memory-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "smoke": "node --check src/worker.js && node --check scripts/live-smoke.mjs && node --check scripts/oauth-login.mjs && node --input-type=module -e \"await import('./src/worker.js'); await import('./scripts/live-smoke.mjs'); await import('./scripts/oauth-login.mjs')\"",
    "live:smoke": "node scripts/live-smoke.mjs",
    "oauth:login": "node scripts/oauth-login.mjs",
    "cf:dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "jose": "6.2.10",
    "zod": "^4.1.5"
  },
  "devDependencies": { "wrangler": "4.127.1" }
}
